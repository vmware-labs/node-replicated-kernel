[package]
name = "bespin"
version = "0.0.1"
authors = [
  "Gerd Zellweger <mail@gerdzellweger.com>",
]

description = "Experimental OS kernel"
readme = "README.md"
license = "MIT"

[dependencies]
x86 = { path = "../lib/x86" }
multiboot = { path = "../lib/multiboot" }
raw-cpuid = { path = "../lib/cpuid" }
rlibc = { path = "../lib/rlib" }
klogger = { path = "../lib/klogger" }
slabmalloc = { path = "../lib/slabmalloc" }
elfloader = { path = "../lib/elfloader" }
backtracer = { path = "../lib/backtracer/" }
#termstyle = { path = "../lib/termstyle" }
lazy_static = { version = "1.1", features = ["nightly", "spin_no_std"] }
fringe = { path = "../lib/libfringe", default-features = false, features = ["alloc"] }
spin = "0.4.8"
log = "0.4"

[dev-dependencies]
rexpect = "0.3"
matches = "0.1.8"

[features]
avc = []
integration-tests = []
test-exit = []
test-pfault = []
test-gpfault = []
test-alloc = []
test-scheduler = []
test-sse = []


#[[test]]
#name = "integration-test"
#path = "tests/integration-test.rs"
