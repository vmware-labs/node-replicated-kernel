/// Modes to create/open the file.
pub const O_RDONLY: u64 = 0x0000; /* open for reading only */
pub const O_WRONLY: u64 = 0x0001; /* open for writing only */
pub const O_RDWR: u64 = 0x0002; /* open for reading and writing */
pub const O_ACCMODE: u64 = 0x0003;
